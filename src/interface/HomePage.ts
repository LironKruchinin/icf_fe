
export interface Boxes {
    imageLink: string,
    boxText: string,
    buttonText: string,
    imageAlt?: string,
    isBlackFont?: boolean
    buttonLink?: string
    boxStyle?: { [key: string]: string }
}